# зависимости
node_modules/

# секреты - реальный .env в репозиторий не попадает, остается только .env.example
.env

# сохраненная сессия (cookies живого сеанса) - тоже не коммитим
/playwright/.auth/

# отчеты и артефакты прогонов playwright
playwright-report/
test-results/
blob-report/
playwright/.cache/

# allure (на будущее)
allure-results/
allure-report/

# системный мусор
.DS_Store
results.json
